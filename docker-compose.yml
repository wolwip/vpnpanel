services:
  vpnpanel:
    build: .
    container_name: vpnpanel
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env
    environment:
      DATA_DIR: "/app/data"
      MONITOR_INTERVAL: "10"
      MONITOR_TIMEOUT: "3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
